---
topic: pulse — personal data hub + dashboarding tool: auto ingestion from public sources, manual CSV drops, DuckDB, hand-built HTML/SVG/JS dashboards on duckdb-wasm, AI data lab
goal: At-a-glance answers on economy/demographics/climate/personal finances for personal use; public repo as portfolio piece; all technical choices challengeable
mode: partner
status: complete
updated: 2026-08-07T12:24
---

- (decision) Stance: Creative Partner (chosen in chat, composer page declined)
- (decision) Batch chosen by coach: Question Storming -> Assumption Reversal -> One Feature Only -> Sci-Fi Artifact From the Future
- (technique) started Question Storming
- (direction) User found Question Storming hard to start; asked coach to generate example questions
- (question by coach) Is France's situation getting better or worse, and faster or slower than last year?
- (question by coach) Which countries are pulling ahead of France on the indicator I care about, and since when?
- (question by coach) What number changed this month that I did NOT expect to change?
- (question by coach) Am I on track, or do I need to do something about it (personal finances)?
- (question by coach) Where did my money actually go last month vs where I think it went?
- (question by coach) Is my carbon number dominated by one thing I could change, or spread thin?
- (question by coach) Do two things I assume are linked (e.g. inflation and my spending) actually move together?
- (question by coach) Is this data still fresh, and did an upstream source silently revise history?
- (question by coach) Which dashboard have I not opened in 6 months (i.e. which question did I stop caring about)?
- (insight by coach) Question types worth covering: level, trend, comparison, correlation, anomaly, decision, and meta-questions about the tool/data itself
- (insight by user) Real problem: not a fixed question list; pulse is a system for cheaply visualizing whatever data he cares about
- (insight by user) Correction: dashboards are NOT churn-prone. One-shot questions go to ChatGPT/Claude; pulse is for standing questions watched over months and years
- (insight by coach) Therefore pulse is an ARCHIVE first, dashboards second: value compounds with time-series depth, so never losing history is the core requirement
- (idea by coach) Store data bitemporally (vintages): what the source said on date T about period P, so upstream revisions are visible rather than silently overwriting history
- (idea by coach) Fatal failure mode for a longitudinal tool: silent ingestion breakage leaving a months-long gap discovered years later; gaps must be visible on the dashboard itself
- (direction) User asks coach to propose the end-to-end flow; user will correct (react rather than generate cold)
- (idea by coach) Unit of extension = a source folder (source.toml + optional fetch script); pulse discovers folders, so adding a source is adding a directory
- (idea by coach) Ingestion writes an immutable raw snapshot per run before any transform, so everything downstream can be replayed/rebuilt from raw
- (idea by coach) One tall observations table (source, series_id, period, value, vintage, unit) as the universal shape; makes every visual read the same interface
- (idea by coach) Publish step exports per-dashboard parquet slices next to the static site; duckdb-wasm range-reads them over HTTP
- (idea by coach) A visual = SQL query + rendering file + manifest; the contract is the query result shape, so Claude Design never needs to know about ingestion
- (idea by coach) A dashboard = a manifest listing visuals + layout; build assembles static HTML
- (question by coach) Public portfolio repo + private bank data: where does private data live, and what does the public version show?
- (decision by user) Raw storage: keep file-based sources in their original format; use parquet when the source is not already a file
- (decision by user) REJECTED one tall observations table: redundancy/size, cannot fit all data shapes, forces everything into one value type
- (idea by user) Introduce DATASETS: curated tables built from raw data targeting specific question areas; queries for visuals are built on datasets
- (idea by user) Give Claude Design the dataset (not just a query result) so it can propose more sophisticated visuals
- (idea by coach) Dataset carries a metadata sidecar (grain, dimensions, measures, units, coverage, known quirks) = small semantic layer; doubles as portfolio documentation and as the agent's briefing
- (idea by coach) Private data: data-root indirection + build profiles - public build excludes private datasets, local 'pulse serve' serves built site plus gitignored private parquet; same dashboard code both ways
- (insight by user) GitHub Actions notifies on failure; failures are a non-issue if actions exit non-zero properly
- (insight by coach) Real risk is not job failure but job SUCCESS with bad data (HTTP 200 empty file, silent schema change, stale period); needs cheap per-source assertions: row count, expected columns, latest period advanced
- (insight by coach) Gotcha: GitHub disables scheduled workflows after 60 days of repo inactivity - hazard for a monthly job on a portfolio repo
- (question by user) Include dbt in the stack, or overkill? Pro: familiar, easy data tests, dependency mgmt if datasets span sources. Con: transformation complexity may not justify it
- (idea by coach) Recommend dbt (dbt-duckdb) - deciding argument is not transform complexity but that it collapses three needs into one familiar artifact: tests, dataset documentation/semantics, dependency DAG
- (idea by coach) dbt schema.yml descriptions ARE the dataset metadata sidecar; dbt manifest.json is machine-readable so it can serve as Claude Design's briefing on available datasets
- (idea by coach) dbt source freshness covers the staleness half of silent breakage; keep ingestion (fetch+snapshot) OUTSIDE dbt, with raw files registered as dbt external sources
- (insight by coach) A documented dbt DAG is legible portfolio evidence, not just plumbing
- (insight by coach) Tall-vs-wide resurfaces INSIDE each dataset: long form suits homogeneous indicator sets, wide suits fact tables - per-dataset grain choice is exactly the freedom the dataset concept buys
- (direction by user) Cannot spec a dataset in the abstract; needs actual data to work from
- (decision by user) No tall tables anywhere, including within a single dataset: wide and properly typed. Schema changes are acceptable - no service-continuity constraint, just needs a defined workflow
- (decision by user) First source to build: World Bank / public macro. Bank data deferred - never yet tried exporting from his bank
- (insight by coach) Consequence: warehouse is DISPOSABLE, raw archive is the only durable thing - every run is a full rebuild from raw, so schema changes need no migrations ever
- (technique) started Assumption Reversal
- (decision by user) Two distinct modes: agent-generated on-the-fly views belong to the DATA LAB; dashboards must be stable artifacts you open and glance at with latest data
- (idea by coach) Steal from the flip: the lab is where a visual is AUTHORED, the dashboard is where it LIVES. Explore in the lab, then 'promote' the view - agent emits query + render + manifest into the repo. Answers the open question of how visuals get created
- (insight by coach) Consequence: lab is a LOCAL agentic tool (full DuckDB + repo write access), dashboards are the read-only static deploy. Clean runtime split, shared datasets
- (insight by coach) A dashboard is stable CODE pointing at fresh DATA: the artifact is committed and versioned, the parquet slices are regenerated and redeployed each ingestion run
- (insight by coach) This raises the data lab from optional feature #3 to the authoring tool for feature #2
- (decision by user) Data lab stays OPTIONAL: agentic querying has unsolved problems (semantic layer for AI, letting user verify results). Dashboard creation workflow must NOT depend on it; lab-to-dashboard promotion is a later bonus only
- (insight by coach) Authoring exploration needs no AI: he is a data engineer, so DuckDB CLI + SQL against a documented dataset IS the exploration surface. The lab was only ever NL convenience
- (idea by coach) Concrete visual contract: Claude Design builds against a mock 'data' array with a declared column schema; wiring = swapping the mock for the real query result. Makes 'create a new visual' cheap without any agent involvement
- (insight by coach) dbt semantic metadata pays off twice: needed now for docs/tests, and it is exactly the semantic layer a future data lab would require - so the lab gets cheaper without being bet on
- (decision) Per-visual vs per-dashboard query: DEFERRED, decidable per dashboard later. Safe to defer precisely because the visual contract (visual receives rows) hides the difference
- (insight by coach) Only cross-filtering (click one visual, filter the others) would force the dashboard-level shared-query choice early
- (technique) started One Feature Only
- (decision by user) REJECTED archive-only v1: he would not use an app without the glance, and both ingestion and visualization are required
- (insight by user) Counter-principle: the archive only compounds if the app gets used, and it only gets used if it is glanceable - so visualization is not downstream of ingestion, it is what keeps ingestion alive
- (idea by coach) Therefore v1 = thin vertical slice: one real source -> one dataset -> one real dashboard, end to end, rather than a deep data layer with a placeholder page
- (direction by user) Abstract creative techniques are not landing; the productive pattern is coach proposes concrete design, user corrects
- (direction) Dropped technique scaffolding by agreement; continuing in proposal/correction mode over unexplored areas
- (idea by coach) Public raw snapshots committed IN the repo is a feature, not a compromise: they are small (KBs-MBs/yr), and anyone cloning can rebuild the entire warehouse and see real dashboards - reproducibility as portfolio evidence
- (idea by coach) Monthly snapshot commits also keep the scheduled GitHub workflow alive, defusing the 60-day inactivity auto-disable
- (insight by coach) Raw splits by sensitivity: public raw in-repo, private raw gitignored + needs its own backup - a local-only durable archive dies with the laptop
- (idea by coach) Dashboard privacy as a flag in dashboard.toml (private=true) so the public build simply omits those dashboards and their slices
- (decision by user) Public data files committed via Git LFS
- (decision by user) Private data stays local-only on one laptop for v1; durable/online storage deferred to a future study
- (insight by coach) LFS watch item: GitHub free tier is ~1GB storage + 1GB/month bandwidth per account, and public-repo clones draw on the OWNER's bandwidth; set lfs:false on Actions checkout steps that do not need snapshots
- (insight by coach) duckdb-wasm cannot run from file:// (workers + fetch restrictions), so 'pulse serve' is mandatory, not a convenience - you cannot double-click the HTML
- (insight by coach) SharedArrayBuffer needs COOP/COEP headers; GitHub Pages cannot set headers while Cloudflare Pages/Netlify can - so host choice affects duckdb-wasm performance mode
- (decision by user) No always-running local service. The dual model (local = static files + tiny web server, remote = static hosting) is kept as an open item to settle at design/implementation time
- (idea by coach) Vintages proposal: datasets build from the LATEST snapshot only, no vintage dimension anywhere; all snapshots stay in raw so revision archaeology is possible but unmodelled; revision analysis becomes an opt-in extra dataset scanning snapshots, per source, not a global tax
- (decision by user) Revisions NOT modelled: build from latest snapshot only; snapshots remain available for the rare case he wants to explore a revision
- (idea by coach) At-a-glance means the page answers before you interact: sparkline + delta tiles over big interactive charts; interaction is drill-on-demand, never required to get the message
- (idea by coach) Monthly cadence makes 'what changed since last month' the natural unit - every visual carries a delta vs previous ingestion
- (idea by coach) Freshness as a first-class UI element: every dashboard stamped 'data as of X', index page lists dashboards with per-source freshness, stale/failed sources visibly marked - this is where silent breakage gets caught, in the UI rather than in an email
- (idea by coach) One dashboard per topic, sized to one screen with no scrolling; an index page is the entry point
- (decision by user) Delta vs last ingestion is NOT the main message: whether a trend continued matters, and he will not remember past values - full history and its evolution must be visible
- (decision by user) REJECTED one-screen/no-scroll: he is a data person, wants detail and several angles on the data; multi-page/scrolling is fine, calling it a report rather than a dashboard is acceptable
- (insight by coach) Reframe: 'at a glance' means ZERO ASSEMBLY, not low density. The competition is the 15 minutes of finding, downloading and plotting - pulse wins by having it pre-composed, however dense
- (idea by coach) A pulse dashboard is really a STANDING ANALYSIS: a curated multi-angle page on one topic, refreshed monthly - which also makes annotations (series breaks, COVID, definition changes) first-class content
- (insight by coach) Consequence: dense multi-angle pages over one dataset tilt the deferred query question toward ONE dashboard-level slice queried many ways - and re-promote duckdb-wasm, which the tile framing had demoted
- (insight by user) Report content is emergent: start with something roughly like the strawman, then discover through use whether it answers his questions or whether new data/indicators/visuals are needed
- (insight by coach) Therefore the dominant operation is AMENDING an existing report, not creating a new one - v1 content does not need to be right, it needs to be cheap to be wrong
- (idea by coach) Concrete design metric: how many files must change to add one indicator to an existing report? (source.toml, staging model, dataset model, schema.yml, query, visual, dashboard.toml = 7 touch points today). Minimising that number is the real ease-of-use target
- (decision by user) REJECTED file-count as the ease metric: a coding agent does the work, not him. The real metric is AGENT LEGIBILITY - can the agent understand the workflow easily (straightforward, existing code to copy, describable in md, encodable as a skill)
- (insight by coach) Inverts a normal instinct: consistency beats concision. Seven identical predictable files beat three clever conditional ones, because repetition is pattern-matchable and cleverness costs agent reasoning
- (idea by coach) The first vertical slice's real job is to be the EXEMPLAR the agent copies - so build the full path end to end early and make it maximally representative
- (idea by coach) Repo carries its own instructions as first-class artifacts: CLAUDE.md plus skills for 'add a source', 'add an indicator', 'add a visual'. For a portfolio repo, being agent-ready is itself the showcase
- (insight by coach) Agent legibility is another argument for dbt: a conventional framework the agent already knows beats a bespoke pipeline it must learn from scratch. Escape hatches like fetch.py stay rare and clearly marked, since each is a branch the agent must reason about
- (decision by user) CORRECTION to the earlier 'consistency beats concision' entries: this must NOT be read as copy/paste duplication. Coding agents reuse common code, especially via skills. Thousands of duplicated lines are not the goal
- (insight) Refined principle: PREDICTABLE STRUCTURE, SHARED IMPLEMENTATION. What repeats is the shape - file layout, naming, wiring steps, conventions. What must not repeat is the code - shared render components, dbt macros, a common visual library
- (insight by coach) Duplication also hurts the agent-legibility metric itself: near-identical files force the agent to diff them to infer the intended pattern, and they drift
- (insight by coach) Counter-tension to hold: over-abstraction is equally agent-hostile. Keep abstractions shallow and domain-named, and EXTRACT them after the second or third visual rather than designing a framework up front
- (idea by coach) Claude Design's real role: build the design SYSTEM once - the visual primitives (time series, comparison, small multiples) and the page shell - not each individual chart. Afterwards a visual is a query plus a small config selecting a primitive and mapping columns, with bespoke JS only when genuinely needed
- (decision by user) REJECTED build-a-design-system-once: being constrained by it forever would be no better than using an existing charting library. Wants Claude Design + coding agents to tailor each visual exactly to the data and the message
- (insight by user) This is the crux of the app: a new way to visualize data, without established good practices - harder reuse is an accepted cost, and good practices will be discovered through use
- (decision by user) Agree on principles now; handle specifics at design and implementation time
- (idea by coach) Resolution: the stable contract is the DATA INTERFACE, not the visual form. Predictable wiring (how a visual receives rows, how it registers on a report) buys agent legibility; everything above that line stays completely free
- (idea by coach) Shared code is opt-in infrastructure (formatting, scales, theme tokens), never a required framework; patterns are extracted only after proving themselves, and any visual may break the pattern
- (insight by coach) Accepted cost of expressiveness-first: visuals will age unevenly and there is no global restyle. Shared theme tokens (colour, type, spacing) are the cheap hedge that keeps pages coherent without constraining form
- (idea by coach) Stack challenge: since the warehouse is disposable and DuckDB queries parquet directly, the persisted .duckdb file may be unnecessary - dbt-duckdb can materialise datasets straight to parquet, making the warehouse a directory of parquet and collapsing the separate publish/export step
- (idea by coach) Stack challenge: make duckdb-wasm OPTIONAL PER REPORT. Default = query results baked to JSON at build time (instant, no wasm payload, works from file://); reports needing open-ended slicing opt into wasm. The data-interface contract makes this switchable per report
- (insight by coach) duckdb-wasm costs: multi-MB payload plus wasm instantiation and parquet load on every page open - directly in tension with 'at a glance', especially on a phone
- (insight by coach) Two ingestion runtimes are unavoidable: GitHub Actions for public sources, local runs for private data that can never leave the laptop - 'executing ingestion easily' therefore has two paths
- (insight by coach) Non-technical two-year regret risk: coupling the public portfolio repo to the personal tool. Portfolio pressure can suppress useful-but-messy hacks and personally revealing indicators; the tool and the showcase pull in different directions
- (decision by user) duckdb-wasm is kept because he finds in-browser real-time analysis genuinely cool and wants excuses to use it more - a stated goal, NOT to be optimised away by a future context on efficiency grounds
- (insight by coach) Third project goal, previously unstated: pulse is also a PLAYGROUND for tech he enjoys, alongside personal utility and portfolio. This reconciles the hand-built visuals and wasm choices and should steer the stack toward interesting-but-defensible over boring
- (idea by coach) Reframe rather than trim: make one report a deliberate wasm FLAGSHIP with bold slicing, and let simple reports bake JSON - optionality is what allows wasm to be used more boldly where it shows off, instead of taxing every page
- (technique) started convergence: MoSCoW scoping of v1
- (idea by coach) Proposed MUST: one public source (World Bank) + source.toml; immutable raw snapshots via LFS; dbt-duckdb staging -> one wide dataset with schema.yml docs + tests; one real France macro report with several hand-built visuals; the data-interface contract; build + pulse serve + public deploy; monthly Action running the whole chain; data-as-of freshness on the page; CLAUDE.md + add-a-* skills
- (idea by coach) Proposed SHOULD: per-source assertions beyond dbt defaults; index page; chart annotations; theme tokens; datasets materialised straight to parquet; ONE duckdb-wasm flagship report
- (idea by coach) Proposed COULD: manual/private ingestion path for bank data; a second and third report
- (idea by coach) Proposed WONT-THIS-TIME: data lab; lab-to-dashboard promotion; revision modelling; cross-filtering; durable private-data storage; multi-source datasets
- (decision by user) MoSCoW scope CONFIRMED as proposed, including duckdb-wasm flagship report in SHOULD rather than MUST
- (decision) Four open items carried forward unresolved: local vs remote serving specifics; host choice (Pages vs Cloudflare re COOP/COEP); per-visual vs per-dashboard query; whether the personal tool and public repo should eventually separate
- (insight by coach) SYNTHESIS 1: three separate rejections (tall table, design-system-once, one-screen dashboard) are one philosophy - refusing premature universal abstraction. Structure follows content, it is never imposed up front. This is why 'principles now, specifics at design time' was the right closing move
- (insight by coach) SYNTHESIS 2: 'the archive only compounds if used, and it is only used if glanceable' is the project's engine - it orders the roadmap, forces the vertical slice, and makes visualization co-equal with ingestion rather than downstream of it
- (insight by coach) SYNTHESIS 3: the three goals (personal utility, portfolio, playground) dissolve most apparent design disagreements - hand-built visuals and wasm look inefficient under goal 1, are justified by goal 2, and are REQUIRED by goal 3. The third goal was never in the pitch and surfaced only under challenge
- (insight by coach) SYNTHESIS 4: expressiveness-first and agent-legibility only look contradictory - the resolution is that the data interface is the contract. Maximum freedom above the line, maximum predictability below it. Most important architectural statement of the session
- (insight by coach) SYNTHESIS 5: the meta questions (freshness, silent revision) were the only ones that landed in the first technique, hours before archive-first was articulated - the earliest signal that the real subject was the INTEGRITY OF THE RECORD, not the content of answers. Freshness-in-the-UI is its direct descendant
- (insight by coach) SYNTHESIS 6: unresolved tension to watch in v1 - if bespoke visuals prove slow to produce even with agents, the accepted-cost bet on harder reuse is the decision that gets revisited first
