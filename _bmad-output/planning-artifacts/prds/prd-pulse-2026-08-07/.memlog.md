---
topic: pulse — personal data hub + purpose-built reports (PRD)
updated: 2026-08-08T15:37
---

- (decision) Intent=Create. Stakes: solo personal tool held to public-portfolio quality bar -> rigor on contracts/conventions, light ceremony; target 5-8 page PRD.
- (decision) Working mode: Coaching path, Vision+Features entry point (capability-first) - Yann thinks in features and contracts.
- (decision) Yann approved spawning web-research subagents for landscape grounding (session config otherwise forbids agents).
- (event) Inputs ingested: docs/initial-pitch.md and brainstorm-intent.md (2026-08-06) - goals, principles, architecture decisions, MoSCoW v1, 4 open items.
- (event) Infra feasibility research returned; filed to addendum section A. Resolves brainstorm open item 2 (host/duckdb-wasm) and confirms scheduled-workflow defusal.
- (event) CONFLICT PENDING: research recommends plain git over Git LFS for raw snapshots (LFS bandwidth billed to repo owner on public clones, hard-block on overage) - contradicts brainstorm decision 'Public raw committed via Git LFS'. Surfaced to Yann, awaiting decision.
- (decision) Open item 2 (host choice) resolvable: single-threaded duckdb-wasm EH bundle needs no COOP/COEP, so GitHub Pages stays viable; Cloudflare Pages only if threading later proves necessary. Awaiting Yann confirmation.
- (event) Landscape research returned; filed to addendum section B. Key: Evidence.dev/Observable Framework already deliver static+DuckDB+code-charts+cron; pulse's uncovered ground is no-chart-vocabulary bespoke visuals, agent-as-author repo shape, and archive-first semantics bundled with publishing.
- (event) Research finding to address in PRD: prior-art personal data warehouses consistently die at the presentation layer - external corroboration of the archive/glance loop as the ordering principle.
- (assumption) PRD must explicitly justify build-vs-Framework (Framework + no-plotting-lib discipline = ~85% of pulse at ~15% cost) so an efficiency-minded reviewer/agent cannot silently reopen it. Awaiting Yann's framing.
- (event) Conventions flagged for adoption rather than invention: annotations-as-data (joined event table, never hardcoded coords), freshness via dbt exposures (as-of date + source + snapshot hash per visual), file-based loader routing, build-time/client-side query split, per-indicator metadata.
- (decision) Git LFS CONFIRMED for public raw snapshots (Yann, reaffirming brainstorm). Rationale: 10GiB/mo quota will not bind at KB-MB snapshot volume; overage scenarios are recoverable. Bandwidth objection withdrawn as overstated at this volume.
- (decision) LFS mitigations accepted as requirements, targeting the clone-and-rebuild portfolio goal rather than quota: (a) README states git-lfs prerequisite and build fails loudly on a pointer-file check rather than letting dbt parse pointer text; (b) Actions checkout sets lfs:true only on steps needing snapshots, lfs:false elsewhere.
- (decision) duckdb-wasm on ALL pages (Yann). Simplifies the brainstorm's 'baked JSON default + wasm opt-in per report': removes dual data-delivery path, per-report flag, and two code paths. Also removes the last Evidence-specific mismatch.
- (decision) Terminology: 'purpose-built visual' is the glossary noun (tailored to one question, no chart-type vocabulary, zero implication about who built it). Replaces 'hand-built' everywhere - Yann does not build them and the PRD must not imply a human in the loop.
- (decision) Authorship split from artifact: PRD says 'agent-authored' (tool-agnostic, survives tool churn); addendum records the concrete Claude Design -> coding-agent pipeline as current implementation.
- (decision) Evidence.dev restored as live architecture candidate; my ruling-out was overconfident. A .svelte file per visual may fit the visual contract BETTER than an inline JS cell (discrete file, declared props interface). Candidates carried to architecture: Evidence.dev, Observable Framework, Vite + own conventions.
- (decision) Layer A corrected (Yann): 'sources as folders' is a structuring convention, NOT a requirement for a bespoke fetch mechanism. A1 fetch mechanism is open to framework provision; A2 archive semantics (immutable dated snapshots, replayable from raw) is the requirement, mechanism open.
- (decision) GitHub Pages viability is determined by the wasm bundle (single-threaded EH, no COOP/COEP), NOT by framework choice - all three candidates emit static output and deploy to Pages. Serving is therefore not a C/D differentiator; decide on authoring ergonomics alone. Gotcha: project pages serve from /reponame/, base-path config required.
- (decision) LFS CONFLICT RESOLVED: Git LFS RETAINED per Yann, overruling research recommendation. Reasons: 10GiB/mo unlikely to bind; both failure branches acceptable; and plain git is not unlimited either (GitHub size guidance + throttling, and GitHub's own advice is LFS for large files) - the last is a genuine correction to my research, not just risk appetite. Recorded in addendum section D with both sides.
- (assumption) LFS watch item (not decisive): bandwidth drain likely comes from scrapers/bots/CI forks, so consumption is NOT correlated with project success - the 'if popular, monetize' escape valve may not fire when the quota does. Mitigations: lfs:false on checkouts not needing contents, $0 budget cap for hard-block over billing, Releases for artifacts >50MB.
- (change) Addendum B.2 cost ledger rewritten: flat 'what pulse would reinvent' list was stale post layer-split and overstated the build side (counted dev server/build/routing as costs though all candidates supply them; counted parquet chunking as flat though Evidence supplies it). Now a per-candidate table. Bottom three rows - freshness plumbing, theming, data-interface contract - are pulse's own regardless.
- (change) Vision section 1.3: framework-gravity paragraph dropped (Yann: verbose fluff). Cost-list paragraph cut from Vision as architecture bookkeeping; only the data-interface-contract sentence survives there. Section 1.3 is now 4 claims: stack isn't novel / visual authoring is the only closed layer / everything below is open / the contract is the contribution / the falsifying condition.
- (event) OWED IN PRD: data-interface contract has no home yet (prd.md still a stub) - must land as a Glossary term plus its own feature section. Flagged so it cannot fall through.
- (change) Feature 7 corrected: I conflated detection with visibility. Detection was already agreed in brainstorm (dbt data tests + source freshness + Should-item per-source assertions: row count, expected columns, latest period advanced). Feature 7 is the VISIBILITY half only. Also retracted my 'not an ops problem' hand-wave - real split is requirement (user-visible: reports show currency, failed/stale sources marked where you look) vs mechanism (dbt, architecture's business).
- (event) OPEN QUESTION raised, unresolved, Yann's call: what happens to the build when a source fails or goes stale? (a) fail build, publish nothing - one broken source takes down all reports; (b) publish all, mark the bad source; (c) publish, mark, and hold last-good data for that source. Real product consequences either way.
- (change) Feature 8 renamed to 'Authoring workflows' - Yann: 'workflow' is the load-bearing word and I dropped it while stripping mechanism (skills/CLAUDE.md). Framed as goal not implementation; mechanism deferred to architecture.
- (decision) CONTRADICTION IN BRAINSTORM, resolved: 'agent legibility... straightforward, existing code to copy' conflicts with 'Predictable structure, shared implementation: what repeats is the SHAPE (file layout, naming, wiring steps, conventions); what must NOT repeat is the CODE (shared render components, dbt macros, common visual library)'. The second is correct; I cited the first. Resolution for PRD: an agent follows a known shape and calls shared implementation - structure repeats, code does not. Guard retained: abstractions stay shallow and domain-named, extracted after the 2nd-3rd visual, never designed up front.
- (decision) Assertion-failure behaviour: option (a) build with the suspect data and mark it. Yann's reasoning: option (b) (rebuild from last passing snapshot) needs real machinery - snapshot selection, tracking last-pass per source, per-source rebuild - for a rare event; right call for enterprise, wrong cost/benefit for a personal tool. (b) recorded as a future goal, not a v1 requirement.
- (decision) No end-to-end visual walkthrough during PRD (Yann: would drag in implementation detail rather than requirements). Data-interface contract specified as an evolvability property instead: accommodating new data/visual kinds may require development but must not require reinventing the system; existing visuals keep working; change is additive and bounded.
- (event) Consequence of wasm-everywhere to reconcile in MVP scope: brainstorm Should-item 'One duckdb-wasm flagship report' is now moot (wasm is on every page); and 'Datasets materialised straight to parquet' likely promotes from Should toward Must since wasm reads parquet directly.
- (decision) GOAL REFRAMED (Yann, major): the target is AMBIENT FAMILIARITY - carrying key numbers in his head the way he did as an econ PhD student - not fast lookup. 'At a glance' implies retrieval; the real goal is retention through frequent incidental exposure. Applies to economic indicators, climate/emissions, and others.
- (decision) Four consequences of the ambient-familiarity reframe: (1) repetition beats depth - design for opened-often-and-briefly; monthly is right for ingestion but may be too slow for exposure; (2) visual stability is a feature and is in TENSION with expressiveness-first - memory attaches to position and form; (3) render magnitude+direction over precision ('about 1%, slowing' not '1.24%'); (4) comparison and annotation are the RETENTION MECHANISM, not decoration - promotes brainstorm's dense/multi-angle and annotations-first-class from stylistic preference to goal-critical.
- (decision) SM-7 Craft: 'I built something functional and elegant, even if I didn't use it much' is a legitimate success criterion (goal 3). Recorded explicitly so usage metrics cannot silently override the craft goal.
- (event) OPEN, Yann's call: (1) is a visual bespoke-once-then-frozen, or open to redesign? SM-1 depends on stability; goal 3 wants freedom. I lean frozen-by-default with deliberate exceptions. (2) Does the index page promote from Should/nav-aid to the primary retention surface (core numbers seen on every visit)?
- (override) Yann rejected (3rd correction of the same error): I keep deriving VISUAL DESIGN RULES from goals. DELETED as requirements: bespoke-once-then-frozen, magnitude-over-precision, comparison-as-retention-mechanism. These are session-time decisions between Yann and Claude Design, made per visual against real data. PRD scope is the ENGINE and WORKFLOWS that produce visuals, never what a visual shows. SM-1 (ambient familiarity) survives - it is an outcome, not a visual spec.
- (decision) Visual stability is NOT a constraint. It will arise naturally most of the time; Yann may deliberately adjust a visual over time to better fit what he wants. Do not write it down as a rule.
- (decision) NEW REQUIREMENT extracted from Yann: cross-visual conventions mechanism. Somewhere for conventions applying across visuals to live - a visual language the authoring agent consumes for coherence. Starts near-empty; conventions added as they EMERGE from use, never predefined; stay revisable. Testable constraint: adding or changing a convention must not require touching every existing visual. Broader than brainstorm's 'theme tokens' Should-item (colour/type) - covers any emergent convention. Same shape as 'abstractions extracted after the 2nd-3rd visual, never designed up front'.
- (override) Yann corrected: refresh cadence and viewing cadence are INDEPENDENT. My claim that 'opened often and briefly' makes monthly refresh look wrong was wrong - for monthly-published data monthly refresh is correct by construction, and nothing stops opening a report more often than it refreshes. Retracted.
- (decision) REQUIREMENT: ingestion cadence is a per-source property. Changing a source monthly->weekly is a scheduling change, not code/architecture. No system-wide cadence assumed. (Replaces the brainstorm's implied global monthly default.)
- (event) Index page: brainstorm mentions it twice (Should-item 'Index page'; watch item 'per-source freshness on the index') but never defines it. My 'does it change nature under SM-1' question was the same visual-spec error a 4th time. PRD-level questions are only: does a top-level surface exist, and is it where per-source freshness lands (feature 7 needs a home). Contents are not PRD business.
- (decision) Homepage confirmed for v1, PROMOTED from brainstorm Should to Must. Does two jobs: navigation to reports, and pipeline state overview. Yann's reasoning: the habitual navigation traffic is what makes the health information actually get seen - he'd never open a status page deliberately. Same 'catch it where you're already looking' logic as feature 7, applied to the operator rather than the reader.
- (decision) Feature 7 WIDENED from 'source freshness visibility' to pipeline state across all three stages: ingestion, warehouse build, report generation. Broader than the brainstorm watch item, which covered per-source freshness only. Renamed 'Homepage and pipeline visibility'. Homepage CONTENTS are UX's business (bmad-ux), not PRD.
- (event) PRD first full draft written: 11 sections, 24 FRs across 8 features, 5 cross-cutting NFRs, 6 UJs, 7 success metrics + 2 counter-metrics, 6 open questions, 4 assumptions. Data-interface contract landed as FR-10 (was flagged as owed). Awaiting Yann's review.
- (override) Yann: LFS concerns are overblown and must take LESS space, not more. Proposed FR on clone-time integrity (pointer-file check failing loudly) REJECTED - README states the git lfs prerequisite and that is sufficient. Bandwidth risk judged extremely unlikely to bite; watch item dropped entirely.
- (change) Addendum: section D deleted (duplicated the LFS decision Yann had already rewritten into A.1); C.5 repointed from D to A.1; A.1 compressed to 3 paragraphs - dropped the pointer-file argument, the two mitigations-as-requirements, and the budget-cap watch item. Also fixed two of my own bugs: orphan duplicate paragraph under D.3, and C.4 wrongly at h2.
- (change) Vocabulary unified on 'freshness' over 'currency' (Yann: 'current' can mean something else). UJ-2 and FR-19 updated; 'Freshness' added to Glossary, which previously defined neither term.
- (event) Four consequences of Yann's PRD edits flagged and left as-is at his discretion: (1) anti-premature-abstraction rule lost its testable consequence in FR-23, survives only as prose in 1.2 and locally in FR-1; (2) FR-14 lost 'change is additive and bounded, not a rewrite of shared machinery'; (3) both counter-metrics SM-C1/SM-C2 deleted; (4) cross-filtering removed from non-goals but still referenced by open question 3.
- (decision) Review triage, Yann's 4 calls: (1) in-browser exploration REQUIRED but opt-in per report -> new FR-25; (2) 'no chart library' rescoped from dependency-tree to AUTHORING/RENDERING, keeping Evidence.dev viable - fixed in section 5 non-goal, FR-12 and section 1.3; (3) source licensing becomes a requirement on the source -> FR-1 and FR-8 consequences; (4) topic coverage left at two named topics, demographics and personal finances NOT restored.
- (change) Applied without asking: Indicator added to Glossary; NFR-6 performance added (goal 1 is a latency goal, wasm single-threaded on every page); FR-19 widened to all five stages incl. publish; NEW FR-26 status surface cannot silently go stale (homepage is generated by the pipeline it monitors - a failed publish left yesterday's green page live, reproducing the exact failure section 4.7 exists to catch); FR-10 given real content incl. empty-rows and query-failure semantics plus schema-break detectability; FR-9 period assertion made release-calendar-relative (would have fired 11 months in 12 on an annual World Bank series); FR-17 privacy made structural - private propagates from SOURCE to report, so forgetting a per-report flag cannot leak; FR-13 gained shared render-time artifact (theme tokens); FR-22 gained schema-change workflow; FR-3 gained on-demand trigger (the pitch's 4th ease target, previously missing) and default-branch commit.
- (change) Also: private-archive mortality added to section 9 risks (the one item contradicting archive-first); OQ-3 rewritten to own cross-filtering's unclassified status; parquet Must gated on addendum A.5 smoke test; 4 broken cross-refs fixed (two dead addendum D links, FR-9 citing FR-19 not FR-21, 'see section 8' pointing at Success Metrics); addendum C.5 gained dashboard->report rename and a Rejected Framings note recording the 4 visual-content attempts.
- (event) Reviewer pass complete. Rubric: strong overall, 17 findings, 0 critical / 4 high. Adversarial: 6 blockers, all verified real. Two reconciliations + rubric INDEPENDENTLY converged on the same top gap - interactivity unspecified, making a fully static site satisfy every FR. Adversarial's best catch: section 5 non-goal eliminated Evidence.dev, a candidate section 10 carries forward. Rejected as overreach: adversarial claim that SM-1/SM-2 are blockers for lacking backing FRs - outcome metrics do not map to single FRs, and designing for exposure frequency was explicitly rejected by Yann.
- (change) Polish applied to both documents (structure then prose, per doc_standards). PRD: assumptions index reordered to document order, hyphenation of -ly adverbs, unclear antecedents, redundancy. Addendum: A.1 condensed further, parallel construction in 4 places, faulty comparison in B.2, 'hand-written SVG' -> 'raw SVG' (the doc's own banned-vocabulary rule). Also fixed a stale cross-ref I created: B.3 claimed git-scraping corroborates the plain-git recommendation, which was not adopted.
- (event) Polish flags left unchanged at reviewer discretion: goal 3's 'may explicitly not be optimised away' reads weaker than a prohibition (Yann's own softening, deliberate); section 1.3 'code-authored charts' uses 'charts' where glossary says 'visual' (describes other products' stacks, not pulse); heading case mixed Title/sentence 9-9; Oxford-mixed spelling (-ise vs -ize).
- (event) PRD finalized. status=final, updated=2026-08-08. Final shape: 11 sections, 26 FRs across 8 features, 6 cross-cutting NFRs, 6 UJs, 7 success metrics, 5 open questions, 5 assumptions. No external_handoffs configured. Next: bmad-ux, bmad-architecture, bmad-create-epics-and-stories.
